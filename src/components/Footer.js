// the component function
const Footer = (props) => {
    const name = props.name
    //The Components Returned JSX
    return <div className="Footer"> This is Footer {name}</div>
}

// export the component
export default Footer
