import Projects from "./Projects"
// the component function
const Main = (props) => {
    
    //The Components Returned JSX
    return <div className="Maine"> This is Main</div> ,<Projects></Projects>
}

// export the component
export default Main
